import { useProfessionalAuth as useProfessionalAuthContext } from '@/contexts/ProfessionalAuthContext';

// Re-export the hook for consistency with existing patterns
export const useProfessionalAuth = useProfessionalAuthContext;
